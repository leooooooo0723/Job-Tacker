# Job Tracker · 需求文档

**版本**：v1.0  
**最后更新**：2026-05

---

## 目录

1. [项目背景](#1-项目背景)
2. [目标用户](#2-目标用户)
3. [核心概念](#3-核心概念)
4. [功能需求](#4-功能需求)
   - 4.1 [认证系统](#41-认证系统)
   - 4.2 [Dashboard · 今日概览](#42-dashboard--今日概览)
   - 4.3 [投递管理](#43-投递管理)
   - 4.4 [公司管理](#44-公司管理)
   - 4.5 [Offer 管理](#45-offer-管理)
   - 4.6 [简历管理](#46-简历管理)
   - 4.7 [AI 助手](#47-ai-助手)
   - 4.8 [账户设置](#48-账户设置)
5. [数据模型](#5-数据模型)
6. [状态流转](#6-状态流转)
7. [接口规范](#7-接口规范)
8. [非功能性需求](#8-非功能性需求)

---

## 1. 项目背景

国内求职流程普遍周期长、公司多、状态复杂。一个应届生秋招期间往往同时追踪几十家公司，面试日程密集，进度容易混乱。现有解决方案（Excel 表格、备忘录）缺乏结构化状态管理，也无法便捷地聚合信息。

Job Tracker 旨在为求职者提供一个轻量、专注的管理工具，覆盖从"首次投递"到"三方签约"的完整链路，并通过 AI 助手降低日常操作成本。

---

## 2. 目标用户

- 参加校招（秋招 / 春招）的应届生
- 同时追踪多家公司、多个岗位的求职者
- 希望用自然语言快速查询"今天有什么面试"、"我总共投了多少家"的用户

---

## 3. 核心概念

| 概念 | 说明 |
|------|------|
| **求职批次（Cycle）** | 代表一轮完整的求职周期，如"2025 秋招"。同一时期只有一个激活批次。数据视图随批次切换，批次间互不干扰。 |
| **公司（Company）** | 目标公司库。投递记录挂在公司下，公司与投递是一对多关系。 |
| **投递记录（Application）** | 某公司某岗位的投递。包含状态、岗位名、工作地点、JD 链接、备注等。 |
| **日程事件（Event）** | 挂在投递记录下的面试 / 笔试 / 测评安排，含时间和链接。 |
| **Offer** | 当投递状态推进到 `offer` 时，可补充薪资、福利等详情。 |

---

## 4. 功能需求

### 4.1 认证系统

#### 注册

- 用户名：唯一，最少 2 个字符
- 密码：最少 4 个字符，需二次确认
- 注册成功后跳转登录页

**页面截图**

<!-- 截图：注册页 /register -->

#### 登录

- 用户名 + 密码登录
- 登录成功后签发 JWT，存入 `localStorage`，跳转 Dashboard
- 未登录访问任何受保护路由自动跳转 `/login`
- 后端返回 401 时，前端自动清除 token 并跳转 `/login`

**页面截图**

<!-- 截图：登录页 /login -->

---

### 4.2 Dashboard · 今日概览

入口：`/`，需登录。

#### 今日日程

- 展示当天所有面试、笔试、测评事件
- 每条事件显示：时间（HH:mm）、类型（面试 / 笔试 / 测评）、公司名、岗位名、外链（可跳转）
- 当天无安排时显示空状态提示

#### 统计卡片

- 总投递数
- 各关键状态的数量：进行中 / Offer / 已结束

#### 状态分布图

- 甜甜圈图（Recharts），展示各投递状态的占比
- 鼠标悬浮显示状态名与数量

#### 批次筛选

- 顶栏 `CycleSelector` 切换求职批次，所有数据随批次联动刷新

**页面截图**

<!-- 截图：Dashboard 整体 -->

<!-- 截图：今日日程区域（有数据） -->

<!-- 截图：状态分布甜甜圈图 -->

---

### 4.3 投递管理

入口：`/applications`，需登录。

#### 投递列表

- 以**公司**为单位分组，每组可展开 / 收起
- 展开后显示该公司下的所有投递记录
- 每条投递显示：职位名、工作地点（base）、状态徽章、投递时间、JD 链接、备注

**页面截图**

<!-- 截图：投递列表（收起状态） -->

<!-- 截图：投递列表（展开状态，显示详情） -->

#### 新增投递

- 选择公司（从公司库选，需先在公司管理中添加）
- 填写：职位名、工作地点、JD 链接、初始状态（默认"已投递"）、备注
- 成功后追加到对应公司分组

#### 状态更新

- 每条投递有状态下拉选择器，直接选择即更新
- 涵盖完整 17 种状态（见 [§6 状态流转](#6-状态流转)）
- 更新后徽章颜色实时变化

**页面截图**

<!-- 截图：状态下拉选择器展开 -->

#### 日程管理（事件）

- 每条投递下可添加多个日程事件
- 事件类型：面试 / 笔试 / 测评
- 填写：时间（日期 + 时分）、会议链接、备注
- 支持编辑和删除单条事件

**页面截图**

<!-- 截图：添加日程事件的表单 -->

#### 备注编辑

- 点击备注区域直接内联编辑，失焦后保存

#### 删除投递

- 二次确认后删除，同时级联删除该投递下的所有日程事件

#### 快捷跳转

- 顶部提供主流招聘平台快速入口：BOSS直聘、牛客、应届生、实习僧等

---

### 4.4 公司管理

入口：`/companies`，需登录。

#### 公司列表

- 卡片式布局，展示公司名、标签、招聘链接、备注

**页面截图**

<!-- 截图：公司列表页 -->

#### 新增 / 编辑公司

- 字段：公司名（唯一）、招聘链接、标签（多选）、备注
- 预设标签：`国企` `银行` `外企` `互联网` `金融` `央企` `民企` `事业单位`
- 标签支持多选

#### 搜索与筛选

- 按公司名实时搜索
- 按标签筛选（可多选）

#### 快速跳转

- 点击招聘链接直接跳转到公司招聘页

---

### 4.5 Offer 管理

入口：`/offers`，需登录。

#### Offer 列表

- 自动汇总所有状态为 `offer` 的投递记录
- 每张 Offer 卡片显示：公司名、岗位名、城市

**页面截图**

<!-- 截图：Offer 列表页 -->

#### Offer 详情编辑

- 可补充：部门、工作城市、薪资（如"25k × 16"）、福利（文本）
- 未填写的字段显示"待填写"徽章，提示完善信息

**页面截图**

<!-- 截图：Offer 详情编辑弹窗 -->

---

### 4.6 简历管理

入口：`/resumes`，需登录。

#### 简历列表

- 展示已上传的简历：文件名、格式（PDF / DOC / DOCX）、大小、上传时间

**页面截图**

<!-- 截图：简历列表页 -->

#### 上传简历

- 支持格式：PDF、DOC、DOCX
- 大小限制：10MB
- 上传后立即出现在列表

#### 预览 / 下载 / 删除

- PDF：浏览器内联预览
- DOC / DOCX：提示下载（浏览器无法内联渲染）
- 删除：确认后从服务器移除文件及数据库记录

#### 外链跳转

- 提供 WonderCV 简历编辑器的快捷入口

---

### 4.7 AI 助手

入口：`/chat`，需登录。

#### 对话界面

- 气泡式对话布局，用户消息右对齐，助手消息左对齐
- 新消息自动滚动到底部
- 对话历史在数据库中持久化，刷新页面后保留

**页面截图**

<!-- 截图：AI 助手对话界面（有对话记录） -->

#### 快捷提示词

- 提供常用问题的一键发送按钮，如：
  - "今天有什么安排？"
  - "我总共投了多少家？"
  - "帮我统计各阶段进度"

**页面截图**

<!-- 截图：快捷提示词按钮区域 -->

#### 支持的操作

AI 通过 function calling 实现以下操作：

| 功能 | 触发示例 |
|------|---------|
| 查询今日日程 | "今天有什么面试？" |
| 统计投递概况 | "我投了多少家？" |
| 添加日程事件 | "帮我记一下明天下午两点字节的一面" |
| 更新投递状态 | "腾讯一面过了，帮我更新一下" |

- 更新状态操作有二次确认机制，AI 先描述要执行的操作，用户回复"确认"后才实际写库

#### 清空历史

- 提供清空全部对话历史的按钮，不可恢复

---

### 4.8 账户设置

入口：`/account`，需登录。

#### 基本信息展示

- 用户名、角色（普通用户 / 管理员）、注册时间

**页面截图**

<!-- 截图：账户设置页 -->

#### 修改密码

- 需填写：旧密码、新密码、确认新密码
- 旧密码验证失败时给出明确提示

#### 注销账户

- 需输入当前用户名进行二次确认
- 确认后永久删除账户及其所有数据，不可恢复
- 注销成功后跳转 `/login`

---

## 5. 数据模型

### User（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| username | VARCHAR(50) | 唯一，不可修改 |
| passwordHash | VARCHAR(255) | BCrypt 加密（强度 12） |
| role | ENUM | `user` / `admin` |
| createdAt | DATETIME | 注册时间 |

### JobCycle（求职批次）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 所属用户 |
| name | VARCHAR(100) | 如"2025 秋招" |
| isActive | BOOLEAN | 当前激活批次，同用户唯一 |
| startDate | DATE | 批次开始日期 |
| endDate | DATE | 批次结束日期（可为空） |
| createdAt | DATETIME | |

### Company（公司）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| name | VARCHAR(100) | 唯一 |
| tags | JSON | 标签数组，如 `["互联网","外企"]` |
| recruitmentUrl | VARCHAR(500) | 招聘官网链接 |
| notes | TEXT | 备注 |
| createdAt | DATETIME | |
| updatedAt | DATETIME | |

### Application（投递记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 所属用户 |
| companyId | UUID | 关联公司 |
| cycleId | UUID | 所属批次 |
| positionName | VARCHAR(200) | 岗位名称 |
| jdLink | VARCHAR(500) | JD 链接 |
| base | VARCHAR(100) | 工作城市 |
| status | VARCHAR(50) | 投递状态，见 §6 |
| failReason | VARCHAR(200) | 失败原因（可为空） |
| appliedAt | DATETIME | 投递时间 |
| notes | TEXT | 备注 |
| updatedAt | DATETIME | |

### Event（日程事件）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| applicationId | UUID | 关联投递记录 |
| type | ENUM | `interview` / `written_test` / `assessment` |
| scheduledAt | DATETIME | 事件时间 |
| link | VARCHAR(500) | 会议链接（可为空） |
| notes | TEXT | 备注（可为空） |
| createdAt | DATETIME | |

### Offer

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| applicationId | UUID | 关联投递记录，唯一 |
| department | VARCHAR(100) | 入职部门 |
| base | VARCHAR(100) | 工作城市 |
| salary | VARCHAR(100) | 薪资描述，如"25k×16" |
| benefits | TEXT | 福利说明 |
| updatedAt | DATETIME | |

### Resume（简历）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| userId | UUID | 所属用户 |
| name | VARCHAR(200) | 展示名称 |
| filename | VARCHAR(255) | 存储文件名 |
| filePath | VARCHAR(500) | 服务器上的文件路径 |
| mimeType | VARCHAR(100) | `application/pdf` 等 |
| size | BIGINT | 文件大小（字节） |
| createdAt | DATETIME | |

### ChatMessage（对话历史）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 自增主键 |
| userId | VARCHAR(36) | 所属用户 |
| role | VARCHAR(20) | `user` / `assistant` |
| content | TEXT | 消息内容 |
| createdAt | DATETIME | |

---

## 6. 状态流转

投递记录共有 **17 种状态**，按求职进程推进：

```
已投递
  ├─→ 待测评 → 已测评
  ├─→ 待笔试 → 已笔试
  ├─→ 待AI面试 → 已AI面试
  ├─→ 待一面 → 已一面
  │              └─→ 待二面 → 已二面
  │                            └─→ 待三面 → 已三面
  │                                          └─→ 待HR面 → 已HR面
  │                                                         └─→ offer → 三方签约
  └─→ （任意阶段可标记为失败，记录失败原因）
```

### 状态颜色规范

| 颜色 | 适用状态 |
|------|---------|
| 灰色 | 已投递 |
| 琥珀色 | 所有「待」系列 |
| 蓝色 | 所有「已」系列 |
| 绿色 | offer、三方签约 |

---

## 7. 接口规范

所有接口以 `/api` 为前缀。除 `/api/auth/**` 外，均需在请求头中携带：

```
Authorization: Bearer <JWT token>
```

### 认证

| 方法 | 路径 | 请求体 | 说明 |
|------|------|--------|------|
| POST | `/api/auth/register` | `{ username, password }` | 注册 |
| POST | `/api/auth/login` | `{ username, password }` | 登录，返回 `{ token }` |

### Dashboard

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/dashboard` | `?cycleId=` 可选 | 今日日程 + 统计数据 |

### 求职批次

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/cycles` | 获取当前用户所有批次 |
| POST | `/api/cycles` | 新建批次 |
| PUT | `/api/cycles/{id}` | 更新批次（含激活/停用） |
| DELETE | `/api/cycles/{id}` | 删除批次 |

### 投递记录

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/api/applications` | `?cycleId=` 可选 | 获取投递列表 |
| POST | `/api/applications` | | 新增投递 |
| PUT | `/api/applications/{id}` | | 更新投递（状态、备注等） |
| DELETE | `/api/applications/{id}` | | 删除投递（级联删除事件） |

### 日程事件

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/events` | 新增事件 |
| PUT | `/api/events/{id}` | 更新事件 |
| DELETE | `/api/events/{id}` | 删除事件 |

### 公司

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/companies` | 获取公司列表 |
| POST | `/api/companies` | 新增公司 |
| PUT | `/api/companies/{id}` | 更新公司信息 |
| DELETE | `/api/companies/{id}` | 删除公司 |

### Offer

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/offers` | 获取 Offer 列表 |
| PUT | `/api/offers/{applicationId}` | 新增或更新 Offer 详情 |

### 简历

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resumes` | 获取简历列表 |
| POST | `/api/resumes` | 上传简历（`multipart/form-data`） |
| GET | `/api/resumes/{id}` | 下载 / 预览简历文件 |
| DELETE | `/api/resumes/{id}` | 删除简历 |

### AI 助手

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/chat` | 获取对话历史 |
| POST | `/api/chat` | 发送消息，返回 `{ text }` |
| DELETE | `/api/chat` | 清空对话历史 |

### 账户

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/account` | 获取当前用户信息 |
| PUT | `/api/account` | 修改密码 |
| DELETE | `/api/account` | 注销账户 |

---

## 8. 非功能性需求

### 安全

- 密码使用 BCrypt 加密（强度 12），不明文存储
- JWT 有效期 7 天，含 `userId`、`username`、`role`
- Spring Security 无状态模式（STATELESS），CSRF 禁用
- 敏感配置（数据库密码、API Key、JWT Secret）通过环境变量注入，不提交到代码仓库

### 文件存储

- 简历文件存储于服务器本地 `uploads/resumes/` 目录
- 文件名以 UUID 重命名，避免冲突和路径遍历攻击
- 单文件上传上限：10MB
- 支持格式：PDF、DOC、DOCX

### 兼容性

- 前端目标浏览器：Chrome / Edge 最新版
- 移动端：不在当前版本支持范围内，布局以桌面端为主

### 性能

- 面向小规模个人使用（单用户 / 少数好友共用）
- 无需特别考虑高并发，数据库连接池默认配置即可满足需求
